{{- if .Values.ingress.enabled }}
apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: mediator-route
  namespace: "{{ .Release.Namespace }}"
  labels:
    {{ template "labels.common" . }}
spec:
  http:
  - name: mediator-rule
    match:
      hosts:
      {{- range .Values.ingress.applicationUrls }}
        - {{ . }}
      {{- end }}
      paths:
      - /mediator
    backends:
       - serviceName: mediator-server-service
         servicePort: 8080
    plugins:
    - name: proxy-rewrite
      enable: true
      config:
        uri: "/"
  - name: mediator-assets-rule
    match:
      hosts:
      {{- range .Values.ingress.applicationUrls }}
        - {{ . }}
      {{- end }}
      paths:
      - /public/webapp-fastopt-bundle.js
    backends:
       - serviceName: mediator-server-service
         servicePort: 8080
    plugins:
    - name: proxy-rewrite
      enable: true
      config:
        uri: "/public/webapp-fastopt-bundle.js"
    {{ template "cors" . }}

---

apiVersion: apisix.apache.org/v2
kind: ApisixRoute
metadata:
  name: mediator-docs-route
  namespace: "{{ .Release.Namespace }}"
  labels:
    {{ template "labels.common" . }}
spec:
  http:
  - name: mediator-docs-rule
    match:
      hosts:
      {{- range .Values.ingress.applicationUrls }}
        - {{ . }}
      {{- end }}
      paths:
      - /docs/mediator/api/*
    backends:
       - serviceName: mediator-server-service
         servicePort: 8080
    plugins:
    - name: proxy-rewrite
      enable: true
      config:
        regex_uri: ["^/docs/mediator/api/(.*)","/api/$1"]
    {{ template "cors" . }}
{{- end -}}